import { Network } from 'lucide-react'
import AreaRootPlaceholder from '@/components/area/AreaRootPlaceholder'

export default function CusteioBRootPage() {
  return <AreaRootPlaceholder areaId="custeio_b" fallbackNome="Custeio Sub-B (Consultoria)" fallbackIcon={<Network size={24} />} />
}
